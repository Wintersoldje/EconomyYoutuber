# 선택: 이미지 검색(Unsplash). 없으면 키워드 카드(텍스트 이미지)로 대체됨.
UNSPLASH_ACCESS_KEY=

# 선택: OpenAI 요약/대본 생성. 없으면 룰베이스 요약으로 동작(퀄리티 낮음).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ffmpeg 바이너리 경로(보통은 그냥 ffmpeg로 잡힘)
FFMPEG_BIN=ffmpeg
